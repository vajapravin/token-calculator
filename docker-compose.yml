services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    environment:
      FRONTEND_ORIGIN: http://vajapravin.me:8080
    restart: unless-stopped
    networks:
      - app-net

  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    environment:
      API_BASE: http://vajapravin.me:3000
    volumes:
      - ./frontend/public:/app/public
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
